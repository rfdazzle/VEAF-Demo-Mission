#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../veaf-mission-creation-tools/src/nodejs/app.js" "$@"
else 
  exec node  "$basedir/../veaf-mission-creation-tools/src/nodejs/app.js" "$@"
fi
